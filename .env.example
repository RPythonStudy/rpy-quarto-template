# .env.example
# 환경변수 설정 예제 파일
# 사용법: 이 파일을 .env로 복사한 후 필요에 따라 수정하세요
# cp .env.example .env

# Python 모듈 경로 설정
PYTHONPATH=/home/ben/projects/quarto-website-template

# 로그 레벨 설정 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 기타 환경변수들 (필요시 추가)
# R_LIBS_USER=./renv/library
# QUARTO_PROJECT_DIR=./quarto
